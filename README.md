# Hex Color Picker – Hex Code Finder

A minimal **hex color picker** built with HTML, CSS, and JavaScript. It lets you pick a color, see the hex code, preview the color, and copy it to your clipboard with one click.

## Live Site & Forking 

This project is designed to run as a GitHub Pages Website.

Once you enable GitHub Pages on this repository (using the `main` branch and `root` folder), your app will be available at:

```text
https://<your-username>.github.io/<your-repo-name>/
